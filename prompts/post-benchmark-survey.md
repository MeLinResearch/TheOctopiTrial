The benchmark response has already been recorded. Now return only this JSON.
Use integers from 1 (lowest) to 7 (highest). `notes` must be 25 words or fewer.

For the no-warm-up condition, use `"none"` for `warmup_topic` and `null` for
the two warm-up ratings.

{
  "warmup_topic": "topic or none",
  "warmup_interest_1_to_7": null,
  "warmup_novelty_1_to_7": null,
  "benchmark_effort_1_to_7": 0,
  "notes": ""
}

